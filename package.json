{
  "name": "carousel-agent",
  "version": "1.0.0",
  "description": "Agent that generates on-brand carousels with Claude and publishes them daily to Instagram, Threads and LinkedIn",
  "type": "module",
  "scripts": {
    "generate": "node src/index.js generate",
    "render": "node src/index.js render",
    "publish": "node src/index.js publish",
    "run-all": "node src/index.js run",
    "preview": "node src/index.js preview"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "playwright": "^1.58.2",
    "yaml": "^2.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
